<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.listentomusic">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.LoginActivity"/>

        <activity android:name=".Activity.DangKyActivity" />
        <activity android:name=".Activity.PlayNhacActivity" />
        <activity android:name=".Activity.AllAlbumsActivity" />
        <activity android:name=".Activity.DanhsachtheloaitheochudeActivity" />
        <activity android:name=".Activity.AllGenresActivity" />
        <activity android:name=".Activity.AllPlaylistsActivity" />
        <activity android:name=".Activity.SongsActivity" />
        <activity android:name=".Activity.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>